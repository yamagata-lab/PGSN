name: PGSN
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pytest
  # Keep the runtime pins in step with pyproject.toml
  - attrs>=25.3
  - cattrs>=25.1
  - treelib>=1.7
  - click>=8.2
  - graphviz
  - python-graphviz
  - typeguard
